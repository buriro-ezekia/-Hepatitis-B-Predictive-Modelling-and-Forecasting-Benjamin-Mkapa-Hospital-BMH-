version: "3.8"
services:
  web:
    build: .
    ports:
      - "8501:8501"
    environment:
      - STREAMLIT_SERVER_ENABLECORS=false
    volumes:
      - .:/app
    depends_on: []
  # Uncomment the block below to enable Postgres for persistence
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: forecasts
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

#volumes:
#  pgdata:
